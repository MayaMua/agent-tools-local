# Media Process Local — optional configuration
# Copy to .env and fill in only if you need authenticated downloads
# (login-gated or age-gated media) via CookieCloud. Leave unset otherwise.
#   cp .env.example .env

COOKIECLOUD_URL=https://your-cookiecloud-host
COOKIECLOUD_UUID=your-uuid
COOKIECLOUD_PASSWORD=your-password
